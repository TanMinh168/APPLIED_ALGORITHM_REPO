#include <bits/stdc++.h>
using namespace std;
const int MAX = 2e6+5;
int n;
int s[MAX], t[MAX], dp[MAX];
int res = -1;

int main() {
    ios::sync_with_stdio(false); cin.tie(0);
    cout.tie(0);
    cin >> n;
    for (int i = 1; i <= n; i++) {
        cin >> s[i] >> t[i];
    }
    for (int i = 1; i <= n; i++) {
        for (int j = )
    }
}
